{
  "name": "student-management-api",
  "version": "1.0.0",
  "description": "Student Management API with MySQL and Hono",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "db:setup": "mysql -u root -p < setup-database.sql"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.1",
    "dotenv": "^16.3.1",
    "hono": "^4.0.1",
    "mysql2": "^3.6.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/mysql2": "^3.3.4",
    "@types/node": "^20.8.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
